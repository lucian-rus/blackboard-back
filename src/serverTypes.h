#ifndef SERVERTYPES_H
#define SERVERTYPES_H

/* defines the actions that the user can take. will only use some for now */
typedef enum {
    Action_UpdateColor = 0,
    Action_UpdateCoordinates,
} serverType_Action_t;

// define enum for each action defined above:
// *update color
// *update coordinates

#endif